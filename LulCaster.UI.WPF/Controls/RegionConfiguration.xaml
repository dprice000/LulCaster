<UserControl x:Name="ctrlRegionConfiguration" x:Class="LulCaster.UI.WPF.Controls.RegionConfiguration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LulCaster.UI.WPF.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>
    <GroupBox Name="GroupingConfigs" Grid.Row="0" Margin="5,8,5,5" Header="Region Configuration">

      <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
          <Label>Label:</Label>
          <TextBox Width="517" Text="{Binding Label}" IsEnabled="{Binding ElementName=ctrlRegionConfiguration, Path=IsControlEnabled}"></TextBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
          <Label>Trigger Text:</Label>
          <TextBox Width="517" Text="{Binding TriggerValue}" IsEnabled="{Binding ElementName=ctrlRegionConfiguration, Path=IsControlEnabled}"></TextBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
          <Label>Sound File:</Label>
          <Label Content="{Binding SoundFilePath}" ToolTip="{Binding SoundFilePath}"></Label>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
          <Button Name="btnSoundBrowser" Click="btnSoundBrowser_Click" IsEnabled="{Binding ElementName=ctrlRegionConfiguration, Path=IsControlEnabled}">Browse</Button>
        </StackPanel>
      </StackPanel>
    </GroupBox>
  </Grid>
</UserControl>
